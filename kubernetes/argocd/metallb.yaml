project: default
source:
  repoURL: https://github.com/metallb/metallb
  path: config/frr-k8s
  targetRevision: main
destination:
  server: https://kubernetes.default.svc
syncPolicy:
  syncOptions:
    - CreateNamespace=true
